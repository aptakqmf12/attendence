import { Container, Typography } from '@mui/material';

const Page = () => {
  return (
    <Container maxWidth="xl" sx={{ paddingTop: '24px', paddingBottom: '24px' }}>
      <Typography variant="h1">EJM.C boiler template!</Typography>
    </Container>
  );
};

export default Page;
